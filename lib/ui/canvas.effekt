import lib/ui/geometry

record Canvas()

def empty() = Canvas()

def applyGeometry { prog: => Unit / Geometry }: Canvas = {
  try prog()
  with Geometry {
    def arrow(from, to) = inspect(from)
    def triangle(center, size) = inspect(center)
  }
  empty()
}