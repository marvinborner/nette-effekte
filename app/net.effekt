import lib/language/ic/term
import lib/net/net
import lib/ui/dirty
import lib/ui/vector

interface NetModel[N, C, O] {
  def empty(): N
  def context(net: N): C
  def load(net: N) { prog: => Unit / emit[Constructor] }: N / Redraw
  def render(net: N): N / Redraw
  def step(net: N): N / Redraw
  def operation(net: N, op: O): N / Redraw
}