import lib/net/net
import lib/ui/vector

interface NetModel[N, C] {
  def empty(): N
  def load(net: N) { prog: => Unit / NetStream }: N
  def render(net: N): N
  def setSize(net: N, size: Vector): N
  def context(net: N): C
}