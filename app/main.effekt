import app/view

import lib/language/ic/parser
import lib/language/ic/term

extern def backend(): Int =
  jsWeb "0"
  js "1"
  llvm "2"

def mainJsWeb() at {io, global}: Unit / {} = {
  start()
}

def main() = backend() match {
  case 0 => mainJsWeb()
  case 1 => <>
  case 2 => <>
  case _ => <>
}