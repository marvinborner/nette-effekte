import test

import test/ic
import test/lc
import test/net
import test/layout
import test/ruler

def main() = mainSuite("nette-tests") {
  test("run suites") {
    assertTrue(true)
    assertFalse(false)

    assertTrue(ic::tests())
    assertTrue(lc::tests())
    assertTrue(net::tests())
    assertTrue(layout::tests())
    assertTrue(ruler::tests())
  }
}